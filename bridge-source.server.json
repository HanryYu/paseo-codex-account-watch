"\"use strict\";\nvar __create = Object.create;\nvar __defProp = Object.defineProperty;\nvar __getOwnPropDesc = Object.getOwnPropertyDescriptor;\nvar __getOwnPropNames = Object.getOwnPropertyNames;\nvar __getProtoOf = Object.getPrototypeOf;\nvar __hasOwnProp = Object.prototype.hasOwnProperty;\nvar __copyProps = (to, from, except, desc) => {\n  if (from && typeof from === \"object\" || typeof from === \"function\") {\n    for (let key of __getOwnPropNames(from))\n      if (!__hasOwnProp.call(to, key) && key !== except)\n        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });\n  }\n  return to;\n};\nvar __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(\n  // If the importer is in node compatibility mode or this is not an ESM\n  // file that has been converted to a CommonJS file using a Babel-\n  // compatible transform (i.e. \"__esModule\" has not been set), then set\n  // \"default\" to the CommonJS \"module.exports\" for node compatibility.\n  isNodeMode || !mod || !mod.__esModule ? __defProp(target, \"default\", { value: mod, enumerable: true }) : target,\n  mod\n));\n\n// bridge.entry.server.ts\nvar import_node_child_process = require(\"node:child_process\");\nvar import_node_net = require(\"node:net\");\nvar import_node_readline = require(\"node:readline\");\nvar import_node_crypto2 = require(\"node:crypto\");\nvar import_promises2 = require(\"node:fs/promises\");\nvar import_node_path = __toESM(require(\"node:path\"));\nvar import_node_os = __toESM(require(\"node:os\"));\n\n// auth.server.ts\nvar import_node_crypto = require(\"node:crypto\");\nvar import_node_fs = require(\"node:fs\");\nvar import_promises = require(\"node:fs/promises\");\nvar unavailable = (reason) => ({\n  status: \"unavailable\",\n  reason\n});\nvar MAX_AUTH_BYTES = 1024 * 1024;\nfunction record(value) {\n  return value !== null && typeof value === \"object\" && !Array.isArray(value) ? value : null;\n}\nfunction string(value) {\n  return typeof value === \"string\" && value.trim().length > 0 ? value.trim() : null;\n}\nfunction fingerprint(value) {\n  return (0, import_node_crypto.createHash)(\"sha256\").update(JSON.stringify(value)).digest(\"hex\");\n}\nfunction claims(token) {\n  if (typeof token !== \"string\") return null;\n  const parts = token.split(\".\");\n  if (parts.length !== 3) return null;\n  try {\n    return record(\n      JSON.parse(Buffer.from(parts[1], \"base64url\").toString(\"utf8\"))\n    );\n  } catch {\n    return null;\n  }\n}\nfunction parseAuth(contents) {\n  let auth;\n  try {\n    auth = record(JSON.parse(contents));\n  } catch {\n    return unavailable(\"Codex auth.json is not valid JSON yet.\");\n  }\n  if (!auth) return unavailable(\"Codex auth.json has an unsupported format.\");\n  const apiKey = string(auth.OPENAI_API_KEY);\n  if (apiKey && auth.auth_mode !== \"chatgpt\") {\n    const key2 = fingerprint([\"api-key\", apiKey]);\n    return {\n      status: \"readable\",\n      identity: {\n        kind: \"api-key\",\n        fingerprint: key2,\n        label: `API credential \\xB7 ${key2.slice(0, 8)}`,\n        email: null\n      }\n    };\n  }\n  const tokens = record(auth.tokens);\n  const id = claims(tokens?.id_token);\n  const access = claims(tokens?.access_token);\n  const authClaims = record(id?.[\"https://api.openai.com/auth\"]) ?? record(access?.[\"https://api.openai.com/auth\"]);\n  const profile = record(id?.[\"https://api.openai.com/profile\"]) ?? record(access?.[\"https://api.openai.com/profile\"]);\n  const accountId = string(tokens?.account_id) ?? string(authClaims?.chatgpt_account_id);\n  const email = string(id?.email) ?? string(profile?.email);\n  const userId = string(authClaims?.chatgpt_user_id) ?? string(id?.sub) ?? string(access?.sub);\n  if (!accountId && !email && !userId) {\n    return unavailable(\n      \"No stable account identity was found. Credential rotation will not be treated as an account switch.\"\n    );\n  }\n  const key = fingerprint([\n    \"chatgpt\",\n    accountId,\n    userId,\n    email?.toLowerCase() ?? null\n  ]);\n  return {\n    status: \"readable\",\n    identity: {\n      kind: \"chatgpt\",\n      fingerprint: key,\n      email,\n      label: email ? `${email}${accountId ? ` \\xB7 ${fingerprint(accountId).slice(0, 6)}` : \"\"}` : `Codex account \\xB7 ${key.slice(0, 8)}`\n    }\n  };\n}\nasync function readAuth(filePath) {\n  try {\n    const file = await (0, import_promises.open)(\n      filePath,\n      import_node_fs.constants.O_RDONLY | import_node_fs.constants.O_NONBLOCK\n    );\n    try {\n      const stat = await file.stat();\n      if (!stat.isFile() || stat.size > MAX_AUTH_BYTES)\n        return unavailable(\"Codex auth.json is not a supported regular file.\");\n      const buffer = Buffer.alloc(MAX_AUTH_BYTES + 1);\n      const { bytesRead } = await file.read(buffer, 0, buffer.length, 0);\n      if (bytesRead > MAX_AUTH_BYTES)\n        return unavailable(\"Codex auth.json exceeds the size limit.\");\n      return parseAuth(buffer.toString(\"utf8\", 0, bytesRead));\n    } finally {\n      await file.close();\n    }\n  } catch (error) {\n    if (error.code === \"ENOENT\") {\n      return {\n        status: \"readable\",\n        identity: {\n          kind: \"signed-out\",\n          fingerprint: \"signed-out\",\n          label: \"Not signed in\",\n          email: null\n        }\n      };\n    }\n    return unavailable(\"Cannot read Codex auth.json on this host.\");\n  }\n}\n\n// account-runtime.server.ts\nfunction accountFromResponse(value) {\n  if (!value || typeof value !== \"object\" || !(\"account\" in value)) return null;\n  const account = value.account;\n  if (account === null)\n    return { kind: \"signed-out\", email: null, label: \"Not signed in\" };\n  if (!account || typeof account !== \"object\" || !(\"type\" in account))\n    return null;\n  if (account.type === \"chatgpt\") {\n    const email = \"email\" in account && typeof account.email === \"string\" ? account.email.trim() || null : null;\n    return {\n      kind: \"chatgpt\",\n      email,\n      label: email ?? \"Codex account (email unavailable)\"\n    };\n  }\n  if (account.type === \"apiKey\")\n    return { kind: \"api-key\", email: null, label: \"API credential\" };\n  return { kind: \"other\", email: null, label: \"Unsupported account type\" };\n}\n\n// bridge.entry.server.ts\nasync function main() {\n  const [stateRoot, commandJson, ...args] = process.argv.slice(2);\n  const command = JSON.parse(commandJson);\n  if (!stateRoot || !Array.isArray(command) || !command.length || !command.every((item) => typeof item === \"string\" && item.length)) {\n    throw new Error(\"Invalid Codex monitor launch configuration\");\n  }\n  const env = { ...process.env };\n  delete env.ELECTRON_RUN_AS_NODE;\n  const agentId = process.env.PASEO_AGENT_ID;\n  const monitored = Boolean(agentId && args.includes(\"app-server\"));\n  if (!monitored) {\n    const child2 = (0, import_node_child_process.spawn)(command[0], [...command.slice(1), ...args], {\n      env,\n      stdio: \"inherit\"\n    });\n    child2.on(\"error\", () => {\n      process.exitCode = 1;\n    });\n    child2.on(\"close\", (code) => {\n      process.exitCode = code ?? 1;\n    });\n    for (const signal of [\"SIGTERM\", \"SIGINT\"])\n      process.on(signal, () => child2.kill(signal));\n    return;\n  }\n  const runId = (0, import_node_crypto2.randomUUID)();\n  const authPath = import_node_path.default.resolve(\n    process.env.CODEX_HOME || import_node_path.default.join(import_node_os.default.homedir(), \".codex\"),\n    \"auth.json\"\n  );\n  const initial = await readAuth(authPath);\n  const runsDir = import_node_path.default.join(stateRoot, \"runs\");\n  await (0, import_promises2.mkdir)(runsDir, { recursive: true, mode: 448 });\n  const recordPath = import_node_path.default.join(runsDir, `${runId}.json`);\n  const secret = (0, import_node_crypto2.randomBytes)(32).toString(\"hex\");\n  const launch = () => (0, import_node_child_process.spawn)(command[0], [...command.slice(1), ...args], {\n    env,\n    stdio: [\"pipe\", \"pipe\", \"inherit\"]\n  });\n  let child;\n  const record2 = {\n    version: 1,\n    runId,\n    agentId,\n    threadId: null,\n    authPath,\n    startedAt: (/* @__PURE__ */ new Date()).toISOString(),\n    launchIdentity: initial.status === \"readable\" ? initial.identity : null,\n    account: null,\n    port: 1,\n    secret\n  };\n  let persistTail = Promise.resolve();\n  const persist = () => {\n    const serialized = JSON.stringify(record2);\n    persistTail = persistTail.then(async () => {\n      await (0, import_promises2.writeFile)(`${recordPath}.tmp`, serialized, { mode: 384 });\n      await (0, import_promises2.rename)(`${recordPath}.tmp`, recordPath);\n    });\n    return persistTail;\n  };\n  let initialized = false;\n  let busy = false;\n  let closing = false;\n  let exited = false;\n  let sequence = 0;\n  const requests = /* @__PURE__ */ new Map();\n  const probes = /* @__PURE__ */ new Map();\n  let resolveClosed;\n  const closed = new Promise((resolve) => {\n    resolveClosed = resolve;\n  });\n  const probeAccount = async () => {\n    if (!initialized || exited || closing)\n      throw new Error(\"Codex runtime is not ready\");\n    const id = `paseo-account-watch:${runId}:${++sequence}`;\n    const result = await new Promise((resolve, reject) => {\n      const timer = setTimeout(() => {\n        probes.delete(id);\n        reject(new Error(\"Codex account/read timed out\"));\n      }, 4e3);\n      probes.set(id, { resolve, reject, timer });\n      child.stdin.write(\n        `${JSON.stringify({ id, method: \"account/read\", params: { refreshToken: false } })}\n`\n      );\n    });\n    record2.account = accountFromResponse(result);\n    await persist();\n    return record2.account;\n  };\n  const server = (0, import_node_net.createServer)((socket) => {\n    socket.setTimeout(6e3, () => socket.destroy());\n    let input2 = \"\";\n    socket.on(\"error\", () => {\n    });\n    socket.on(\"data\", (chunk) => {\n      input2 += chunk.toString();\n      if (Buffer.byteLength(input2) > 4096) {\n        socket.destroy();\n        return;\n      }\n      if (!input2.includes(\"\\n\")) return;\n      socket.pause();\n      void (async () => {\n        const request = JSON.parse(input2.slice(0, input2.indexOf(\"\\n\")));\n        if (typeof request.secret !== \"string\" || request.secret.length !== secret.length || !(0, import_node_crypto2.timingSafeEqual)(Buffer.from(request.secret), Buffer.from(secret))) {\n          throw new Error(\"Unauthorized runtime request\");\n        }\n        if (request.runId !== runId || request.agentId !== agentId)\n          throw new Error(\"Runtime identity changed\");\n        if (request.operation === \"account\") {\n          const account = await probeAccount();\n          socket.end(\n            `${JSON.stringify({ ok: true, runId, agentId, threadId: record2.threadId, account, busy })}\n`\n          );\n        } else if (request.operation === \"close\") {\n          if (!record2.threadId || request.threadId !== record2.threadId)\n            throw new Error(\"Thread identity changed\");\n          if (busy || requests.size || closing)\n            throw new Error(\"Agent is busy; wait for the turn to finish\");\n          closing = true;\n          child.kill(\"SIGTERM\");\n          let timer;\n          try {\n            await Promise.race([\n              closed,\n              new Promise((_, reject) => {\n                timer = setTimeout(\n                  () => reject(\n                    new Error(\n                      \"Previous Codex process did not exit; reload was not attempted\"\n                    )\n                  ),\n                  4500\n                );\n              })\n            ]);\n          } finally {\n            clearTimeout(timer);\n          }\n          socket.end(\n            `${JSON.stringify({ ok: true, runId, agentId, closed: true })}\n`\n          );\n        } else throw new Error(\"Unknown runtime request\");\n      })().catch(\n        (error) => socket.end(\n          `${JSON.stringify({ ok: false, error: error instanceof SyntaxError ? \"Invalid runtime request\" : error.message })}\n`\n        )\n      );\n    });\n  });\n  await new Promise((resolve, reject) => {\n    server.once(\"error\", reject);\n    server.listen(0, \"127.0.0.1\", resolve);\n  });\n  const address = server.address();\n  if (!address || typeof address === \"string\")\n    throw new Error(\"Runtime control channel unavailable\");\n  record2.port = address.port;\n  child = launch();\n  const input = (0, import_node_readline.createInterface)({ input: process.stdin, crlfDelay: Infinity });\n  const output = (0, import_node_readline.createInterface)({ input: child.stdout, crlfDelay: Infinity });\n  input.on(\"line\", (line) => {\n    let activateProbe = false;\n    try {\n      const message = JSON.parse(line);\n      if (closing) {\n        if (message.id !== void 0)\n          process.stdout.write(\n            `${JSON.stringify({ id: message.id, error: { code: -32e3, message: \"Agent account reload is in progress\" } })}\n`\n          );\n        return;\n      }\n      if (message.id !== void 0 && typeof message.method === \"string\") {\n        requests.set(message.id, message.method);\n        if (message.method === \"turn/start\") busy = true;\n      }\n      if (message.method === \"initialized\") {\n        initialized = true;\n        activateProbe = true;\n      }\n    } catch {\n    }\n    if (!child.stdin.write(`${line}\n`)) {\n      input.pause();\n      child.stdin.once(\"drain\", () => input.resume());\n    }\n    if (activateProbe) void probeAccount().catch(() => {\n    });\n  });\n  child.stdin.on(\"error\", () => {\n  });\n  output.on(\"line\", (line) => {\n    try {\n      const message = JSON.parse(line);\n      const probe = probes.get(message.id);\n      if (probe) {\n        probes.delete(message.id);\n        clearTimeout(probe.timer);\n        if (message.error)\n          probe.reject(new Error(\"Codex could not report its active account\"));\n        else probe.resolve(message.result);\n        return;\n      }\n      if (typeof message.id === \"string\" && message.id.startsWith(`paseo-account-watch:${runId}:`))\n        return;\n      const method = requests.get(message.id);\n      requests.delete(message.id);\n      if ((method === \"thread/start\" || method === \"thread/resume\") && typeof message.result?.thread?.id === \"string\") {\n        record2.threadId = message.result.thread.id;\n        void persist().catch(() => {\n        });\n      }\n      if (method === \"turn/start\" && message.error) busy = false;\n      if (message.method === \"turn/started\") busy = true;\n      if (message.method === \"turn/completed\") busy = false;\n    } catch {\n    }\n    if (!process.stdout.write(`${line}\n`)) {\n      output.pause();\n      process.stdout.once(\"drain\", () => output.resume());\n    }\n  });\n  input.on(\"close\", () => {\n    closing = true;\n    child.kill(\"SIGTERM\");\n  });\n  for (const signal of [\"SIGTERM\", \"SIGINT\"])\n    process.on(signal, () => {\n      closing = true;\n      child.kill(signal);\n    });\n  child.on(\"error\", () => {\n    process.stderr.write(\n      \"Codex monitor could not launch the configured command.\\n\"\n    );\n  });\n  child.on(\"close\", (code) => {\n    exited = true;\n    resolveClosed();\n    input.close();\n    for (const probe of probes.values()) {\n      clearTimeout(probe.timer);\n      probe.reject(new Error(\"Codex process exited\"));\n    }\n    probes.clear();\n    void persistTail.catch(() => {\n    }).then(() => (0, import_promises2.rm)(recordPath, { force: true })).finally(() => {\n      server.close();\n      process.exitCode = code ?? 0;\n      process.stdin.destroy();\n    });\n  });\n  await persist();\n}\nvoid main().catch(() => {\n  process.stderr.write(\"Codex account monitor could not start.\\n\");\n  process.exitCode = 1;\n});\n"
